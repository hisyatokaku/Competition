N,K = map(int,raw_input().split())
R = map(int,raw_input().split())

R = sorted(R)[-K:]
N,K = map(int,raw_input().split())
R = map(int,raw_input().split())

R = sorted(R)[-K:]

def solve(R):
        rate = 0
            for i in R:
                        if i < rate:
                                        return rate
                        else:
                                        rate = (i+rate)/2.
                                            return rate
                                        print solve(R)
                                        
def solve(R):
    rate = 0
    for i in R:
        if i < rate:
            return rate
        else:
            rate = (i+rate)/2.
    return rate
print solve(R)
